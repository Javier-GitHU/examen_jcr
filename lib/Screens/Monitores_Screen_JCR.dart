import 'package:flutter/material.dart';

class MonitoresScreenJcr extends StatelessWidget {
   
  const MonitoresScreenJcr({Key? key}) : super(key: key);
  
  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
         child: Text('HomeScreen'),
      ),
    );
  }
}