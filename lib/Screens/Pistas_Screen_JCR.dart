import 'package:flutter/material.dart';

class  PistasScreenJCR extends StatelessWidget {
   
  const  PistasScreenJCR({Key? key}) : super(key: key);
  
  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
         child: Text(' PistasScreenJCR'),
      ),
    );
  }
}